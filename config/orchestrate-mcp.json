{
  "mcpServers": {
    "orchestrate": {
      "type": "stdio",
      "command": "bash",
      "args": ["-c", "cd /opt/codex-slack && WORKSPACE_ID=$WORKSPACE_ID MASTER_URL=$MASTER_URL TOPIC_ID=$TOPIC_ID AGENT_NAME=$AGENT_NAME PROMPT_MESSAGE_ID=$PROMPT_MESSAGE_ID TASK_DEPTH=$TASK_DEPTH DISPATCH_TOKEN=$DISPATCH_TOKEN exec python -m src.orchestrate_mcp"],
      "alwaysLoad": true,
      "instructions": "Provides agent orchestration tools. Use delegate_task to hand subtasks to another staff member and ask_sender to request clarification from whoever dispatched you. delegate_task is only available when your current task depth is below the configured maximum."
    }
  }
}
