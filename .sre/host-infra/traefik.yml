api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    network: sre-traefik-public
    exposedByDefault: false
    watch: true

log:
  level: INFO
