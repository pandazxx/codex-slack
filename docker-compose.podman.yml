services:
  codex-slack-bot:
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./:/workspace:Z,U
      - ./logs:/workspace/logs:Z,U
      - ${HOME}/.codex/auth.json:/run/secrets/codex_auth.json:ro,Z
